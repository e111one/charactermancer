export type Config = {
    name: string,
    menuName: string,
    progressionRepositoryName: string
}

export const Config: Config = {
    name: "charactermancer",
    menuName: "charactermancer-settings",
    progressionRepositoryName: "charactermancer-prog"
}